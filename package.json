{
  "name": "nightcode",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --watch -r @nightcode/cli",
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}